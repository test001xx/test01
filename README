1. solidity.js - исходный код смарт-контракта на €зыке Solidity. ¬ Browser Solidity отрабатывал как положено, провер€л.

2. test.js - скрипт node.js, с помощью которого производилс€ деплой исходного кода смарт-контракта из Browser Solidity.

3. akks.txt - текстовый файл с тестовыми ключами

4. ƒл€ сервера node.js были установлены следующие модули:
- web3
- ethereumjs-util
- ethereumjs-tx
- crypto-js

5. node.js - файл дл€ запуска backend-сервера node.js на локальной машине. –еализации на php не нашел, поэтому было сделано на nodejs (на нем ранее не работал со всеми вытекающими последстви€ми). ¬ отличие от Browser Solidity, с тестовой сетью Infura на хакатоне возникли проблемы с получением значений из функций, опубликованных в смарт-контракте. ѕоэтому в исходном коде node.js есть р€д неубранного отладочного кода. ѕо той же причине index.html не отрефакторен.

6. index.html - файл с frontend-частью, доделать не успел по причине проблем с тестовой сетью Infura.

7. web3.min.js и jquery-2.2.3.min.js - дополнительные библиотеки дл€ работы исходного кода.
